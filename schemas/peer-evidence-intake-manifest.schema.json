{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.invalid/quest-termux-lab/peer-evidence-intake-manifest.schema.json",
  "title": "Quest Termux Lab Peer Evidence Intake Manifest",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema",
    "fleet_id",
    "source_agent_id",
    "observed_at",
    "intake_id",
    "experiment_scope",
    "require_rehearsal_ready",
    "required_artifact_kinds",
    "optional_artifact_kinds",
    "artifact_paths",
    "authority_boundary"
  ],
  "properties": {
    "schema": {
      "const": "quest-termux-lab.peer-evidence-intake-manifest.v1"
    },
    "fleet_id": {
      "type": "string",
      "minLength": 1
    },
    "source_agent_id": {
      "type": "string",
      "minLength": 1
    },
    "observed_at": {
      "type": "string",
      "format": "date-time"
    },
    "intake_id": {
      "type": "string",
      "minLength": 1
    },
    "experiment_scope": {
      "const": "private_configured_peer_gossip_only"
    },
    "require_rehearsal_ready": {
      "type": "boolean"
    },
    "required_artifact_kinds": {
      "type": "array",
      "uniqueItems": true,
      "items": {
        "enum": [
          "rehearsal_report",
          "trust_report",
          "gossip_receipt",
          "route_health_report",
          "route_history_report",
          "cleanup_record"
        ]
      }
    },
    "optional_artifact_kinds": {
      "type": "array",
      "uniqueItems": true,
      "items": {
        "enum": [
          "rehearsal_report",
          "trust_report",
          "gossip_receipt",
          "route_health_report",
          "route_history_report",
          "cleanup_record"
        ]
      }
    },
    "artifact_paths": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "rehearsal_report": {
          "type": ["string", "array"],
          "items": {
            "type": "string",
            "pattern": "^(?!.*\\.\\.)[a-zA-Z0-9_./-]+$"
          }
        },
        "trust_report": {
          "type": ["string", "array"],
          "items": {
            "type": "string",
            "pattern": "^(?!.*\\.\\.)[a-zA-Z0-9_./-]+$"
          }
        },
        "gossip_receipt": {
          "type": ["string", "array"],
          "items": {
            "type": "string",
            "pattern": "^(?!.*\\.\\.)[a-zA-Z0-9_./-]+$"
          }
        },
        "route_health_report": {
          "type": ["string", "array"],
          "items": {
            "type": "string",
            "pattern": "^(?!.*\\.\\.)[a-zA-Z0-9_./-]+$"
          }
        },
        "route_history_report": {
          "type": ["string", "array"],
          "items": {
            "type": "string",
            "pattern": "^(?!.*\\.\\.)[a-zA-Z0-9_./-]+$"
          }
        },
        "cleanup_record": {
          "type": ["string", "array"],
          "items": {
            "type": "string",
            "pattern": "^(?!.*\\.\\.)[a-zA-Z0-9_./-]+$"
          }
        }
      }
    },
    "authority_boundary": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 1
      }
    }
  }
}
